{
  "parameters": {
    "alpha": ".1",
    "I": ".25"
  },

  "dynamics": [
    {
      "expression": "V' = I + 2 * W * (-.7 - V) + .5 * (-.5 - V) + 1.1 * (.5 * (1 + tanh((V + .01) / .15))) * (1 - V)",
      "initial_value": "1"
    },
    {
      "expression": "W' = alpha * cosh((V - .22) / .6) * (.5 * (1 + tanh((V + .12) / .3)) - W)",
      "initial_value": "1"
    }
  ]
}
